# Suno AI Music Landing Page

Landing page для AI-генерации музыки на базе FastAPI с Telegram уведомлениями.

## 📁 Структура проекта

```
lending/
├── main.py              # FastAPI backend с БД заявок
├── bot.py               # Telegram bot для уведомлений админу
├── admin.py             # Flask админ-панель (альтернатива)
├── requirements.txt     # Python зависимости
├── Dockerfile           # Docker образ
├── docker-compose.yml   # Docker Compose конфигурация
├── .env.example         # Пример переменных окружения
├── data/               # База данных SQLite
│   └── leads.db
└── templates/
    └── static/
        ├── index.html       # Landing page
        ├── admin.html       # Админ-панель (FastAPI)
        ├── style.css        # Стили
        └── app.js           # JavaScript
```

## 🚀 Быстрый старт

### 1. Настройка окружения

.env файл уже создан, нужно только заполнить переменные окружения.

### 2. Запуск через Docker

```bash
# Только backend + landing
docker-compose up -d
```

### 3. Локальный запуск (без Docker)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск backend
uvicorn main:app --reload

# В другом терминале - запуск бота
python bot.py
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательная |
|-----------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | Да (для бота) |
| `TELEGRAM_ADMIN_ID` | Ваш Telegram ID | Да (для уведомлений) |
| `SUNO_API_KEY` | API ключ Suno | Нет |
| `ADMIN_PASSWORD` | Пароль для админ-панели | Нет (по умолчанию `admin123`) |

### Получение Telegram ID

1. Напишите боту @userinfobot
2. Он пришлет ваш ID
3. Запишите его в `TELEGRAM_ADMIN_ID`

## 🎨 Landing Page секции

1. **Hero** - Главный экран с призывом к действию
2. **Benefits** - 6 преимуществ сервиса
3. **How It Works** - 3 шага работы
4. **Testimonials** - Отзывы клиентов
5. **Contact Form** - Форма заявки с сохранением в БД

## 📱 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/leads` | POST | Создание заявки |
| `/api/leads` | GET | Список заявок (с пагинацией) |
| `/api/leads/{lead_id}` | GET | Детали заявки |
| `/api/stats` | GET | Статистика |
| `/api/generate` | POST | Генерация музыки |
| `/api/admin/login` | POST | Авторизация в админ-панели |

## 🔐 Админ-панель

Доступна по адресу `http://localhost:8000/admin`

- **Пароль**: из переменной `ADMIN_PASSWORD` в `.env` (по умолчанию `admin123`)
- **Функции**: просмотр заявок, статистика, поиск, детали
- **Технологии**: FastAPI + Alpine.js + Tailwind CSS

## 🐳 Docker

```bash
# Сборка
docker-compose build

# Запуск
docker-compose up -d

# Логи
docker-compose logs -f backend
docker-compose logs -f bot

# Остановка
docker-compose down
```

## 📦 Базы данных

- **SQLite** - локальное хранилище заявок (`./data/leads.db`)
- Миграции автоматические

## 🔔 Уведомления в Telegram

При создании заявки через форму лендинга:
1. Заявка сохраняется в БД
2. Отправляется уведомление админу в Telegram
3. Содержит: имя, email, телефон, сообщение, время
 
 

## 📝 Лицензия

MIT
