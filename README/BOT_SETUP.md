# 🤖 Инструкция по настройке Telegram бота

## Шаг 1: Создание бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте название бота (например: "Suno AI Notifications")
4. Придумайте username для бота (должен заканчиваться на "bot", например: "suno_ai_notify_bot")
5. BotFather пришлет вам **токен бота** (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получение вашего Telegram ID

1. Найдите в Telegram бота **@userinfobot**
2. Отправьте ему любое сообщение или нажмите /start
3. Бот ответит вашим ID (например: `123456789`)

**Важно:** Это числовой ID, не ваш username!

## Шаг 3: Настройка .env файла

Создайте файл `.env` в корне проекта (или отредактируйте существующий):

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_ADMIN_ID=123456789

# Suno API (опционально)
SUNO_API_KEY=your_suno_key_here
```

**Важно:** Замените значения на ваши реальные токен и ID!

## Шаг 4: Запуск бота

### Вариант 1: Docker (рекомендуется)

```bash
# Остановите текущие контейнеры
docker-compose down

# Запустите с ботом
docker-compose --profile with-bot up -d
```

### Вариант 2: Локально

```bash
# В одном терминале - backend
cd /Users/egordudov/Documents/docs_job/lending
uvicorn main:app --reload

# В другом терминале - bot
python bot.py
```

## Шаг 5: Проверка бота

1. Найдите своего бота в Telegram (по username который создали)
2. Отправьте `/start`
3. Бот должен ответить сообщением о том, что вы администратор
4. Отправьте `/status` для проверки конфигурации

## Шаг 6: Тестирование уведомлений

1. Откройте лендинг: http://localhost:8000
2. Заполните форму заявки
3. Отправьте заявку
4. Проверьте Telegram — должно прийти уведомление!

## 📋 Формат уведомления в Telegram

```
🔔 НОВАЯ ЗАЯВКА С ЛЕНДИНГА!

👤 Имя: Иван Иванов
📧 Email: ivan@example.com
📱 Телефон: +7 (999) 123-45-67

🎵 Музыкальный стиль: 🎸 Рок
📝 Нужен текст: ✅ Да
📄 Описание текста: О любви, энергичный

💬 Комментарий: Для рекламного ролика

🕐 Время: 18.02.2024 14:30
📊 ID заявки: #42
```

## ⚠️ Устранение неполадок

### Бот не присылает уведомления

1. Проверьте логи: `docker-compose logs -f bot`
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ADMIN_ID` заполнены
3. Проверьте, что вы написали `/start` боту
4. Проверьте, что ID владельца совпадает с `TELEGRAM_ADMIN_ID`

### "Доступ запрещен" при /start

- Ваш Telegram ID не совпадает с `TELEGRAM_ADMIN_ID`
- Исправьте ID в .env файле

### Бот не запускается

```bash
# Пересоберите контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose --profile with-bot up -d
```

## 📝 Дополнительные команды бота

- `/start` — Проверка доступа и приветствие
- `/help` — Информация о боте
- `/status` — Проверка статуса конфигурации

## 🔐 Безопасность

**Никогда не публикуйте:**
- `TELEGRAM_BOT_TOKEN` — это ключ от вашего бота
- Файл `.env` в публичных репозиториях
- Скриншоты с токеном

**Добавьте в .gitignore:**
```
.env
data/
*.db
```

## 🎯 Готово!

Теперь при каждой новой заявке с лендинга вы будете получать мгновенное уведомление в Telegram с полной информацией о клиенте.
