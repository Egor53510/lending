# Админ-панель Suno AI

## Обзор
Админ-панель для просмотра и управления заявками из базы данных SQLite с авторизацией по паролю.

## Доступ к админ-панели

### Вариант 1: FastAPI интеграция (рекомендуется)
Админ-панель встроена в основное приложение и доступна по адресу:
- `http://localhost:8000/admin`

### Вариант 2: Отдельное Flask приложение
Альтернативный вариант с отдельным процессом:
```bash
python admin.py
```
Доступно по адресу: `http://localhost:5001`

## Функционал

### 🔐 Авторизация
- Вход по паролю из переменной окружения `ADMIN_PASSWORD`
- Пароль по умолчанию: `admin123`
- Защищенная сессия

### 📊 Дашборд
- Статистика заявок (всего, сегодня, конверсия)
- Таблица со всеми заявками
- Поиск по имени, email или стилю
- Цветовая индикация музыкальных стилей

### 📋 Детальная информация
- Полная информация о каждой заявке
- Кнопки для копирования контактов
- Прямая ссылка для ответа на email
- Отображение всех полей формы

### 🎨 Дизайн
- Glass morphism эффекты
- Адаптивный интерфейс
- Темная тема с градиентами
- Font Awesome иконки

## Структура URL

- `/admin` - главная страница (редирект на вход/дашборд)
- `/admin/login` - страница входа
- `/admin/dashboard` - дашборд со списком заявок
- `/admin/lead/<id>` - детальная информация о заявке
- `/admin/logout` - выход из системы

## Безопасность

- Пароль хранится в переменных окружения (.env файл)
- Защита от CSRF через Flask сессии
- Проверка авторизации для всех админных страниц

## Переменные окружения

В файле `.env`:
```
ADMIN_PASSWORD=admin123  # Пароль для входа в админ-панель
```

## База данных

Используется существующая база данных `data/leads.db`
Таблица `leads` с полями:
- id, name, email, phone, style, has_text, text_description, message, created_at

## Технологии

### FastAPI интеграция (основной вариант)
- **Backend**: FastAPI (Python)
- **Frontend**: Tailwind CSS + Alpine.js
- **Database**: SQLite
- **Auth**: JWT токены через API

### Flask приложение (альтернативный вариант)
- **Backend**: Flask (Python)
- **Frontend**: Tailwind CSS + Alpine.js
- **Database**: SQLite
- **Auth**: Flask сессии
