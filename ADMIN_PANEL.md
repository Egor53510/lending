# Админ-панель Suno AI

## Обзор
Простая админ-панель для просмотра заявок из базы данных SQLite с авторизацией по паролю. Весь функционал доступен через один эндпоинт `/admin`.

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements_admin.txt
```

### 2. Запуск админ-панели
```bash
python admin.py
```

Админ-панель будет доступна по адресу: `http://localhost:5001/admin`

## Функционал

### 🔐 Авторизация
- Вход по паролю из переменной окружения `ADMIN_PASSWORD`
- Пароль по умолчанию: `admin123`
- Выход через параметр `?logout=1`

### 📊 Дашборд
- Статистика заявок (всего, сегодня, конверсия)
- Таблица со всеми заявками
- Поиск по имени, email или стилю
- Цветовая индикация музыкальных стилей

### 📋 Детальная информация
- Полная информация о каждой заявке
- Кнопки для копирования контактов
- Прямая ссылка для ответа на email
- Отображение всех полей формы

### 🎨 Дизайн
- Glass morphism эффекты
- Адаптивный интерфейс
- Темная тема с градиентами
- Font Awesome иконки

## Структура URL

**Единый эндпоинт `/admin`:**
- `GET /admin` - показывает форму входа или дашборд (если авторизован)
- `POST /admin` - обработка входа
- `GET /admin?logout=1` - выход из системы
- `GET /admin/lead/<id>` - детальная информация о заявке

## Безопасность

- Пароль хранится в переменных окружения (.env файл)
- Защита от CSRF через Flask сессии
- Проверка авторизации для админных страниц

## Переменные окружения

В файле `.env`:
```
ADMIN_PASSWORD=admin123  # Пароль для входа в админ-панель
```

## База данных

Используется существующая база данных `data/leads.db`
Таблица `leads` с полями:
- id, name, email, phone, style, has_text, text_description, message, created_at

## Технологии

- **Backend**: Flask (Python)
- **Frontend**: Tailwind CSS + Alpine.js
- **Database**: SQLite
- **Auth**: Flask сессии

## Преимущества упрощенной версии

- ✅ Один эндпоинт вместо четырёх
- ✅ Простой код без лишних декораторов
- ✅ Легкая навигация
- ✅ Минимум зависимостей
